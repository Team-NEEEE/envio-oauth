name: Build Dev Image (CI)

on:
  push:
    # 테스트용 브랜치
    branches: [ "develop-infra" ]

jobs:
  build-dev:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Docker Hub login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build dev image
        run: docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/oauth-app:dev ./OAuth

      - name: Push dev image
        run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/oauth-app:dev